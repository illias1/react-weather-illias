import React from 'react'
import Typography from '@material-ui/core/Typography';

export default () => {
    return (
        <Typography variant="h1">
            A simple weather forecast using Openweathermap.org API
        </Typography>
    )
}